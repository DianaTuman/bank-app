server:
  port: 8086

spring:
  config:
    import: "kubernetes:"
  application:
    name: notifications-service            # Имя сервиса в реестре Consul

  cloud:
    kubernetes:
      discovery:
        enabled: true
      config:
        enabled: true