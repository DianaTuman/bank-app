apiVersion: v1
kind: ConfigMap
metadata:
  name: cash-service
data:
  application.properties: |
    accounts_service_url={{ include "bank.accountsServiceURL" . }}
    blocker_service_url={{ include "bank.blockerServiceURL" . }}
    bank_app_notifications_topic={{ include "bank.notifTopic" . }}