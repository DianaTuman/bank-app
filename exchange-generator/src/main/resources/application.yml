server:
  port: 8085

spring:
  config:
    import: "kubernetes:"
  application:
    name: exchange-generator-service

  cloud:
    kubernetes:
      discovery:
        enabled: true
      config:
        enabled: true