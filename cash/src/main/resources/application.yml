logging:
  level:
    org.springframework.web.client.RestTemplate: DEBUG

server:
  port: 8083

spring:
  config:
    import: "kubernetes:"
  application:
    name: cash-service

  cloud:
    kubernetes:
      discovery:
        enabled: true
      config:
        enabled: true