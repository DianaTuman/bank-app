server:
  port: 8083

spring:
  config:
    import: "kubernetes:"
  application:
    name: cash-service            # Имя сервиса в реестре Consul

  cloud:
    kubernetes:
      discovery:
        enabled: true
      config:
        enabled: true